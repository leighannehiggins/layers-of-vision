---
description: "Layers of Vision is an art exhibition that explores the experiences and perspectives of blind and partially sighted (BPS) artists living in a world made for sighted people."
pageImageWebPath: "/assets/img/logos/lov-temp-logo.png"
---
<!DOCTYPE html>
<html lang="en-GB">
  <head>
    <meta charset="utf-8">
    <!-- Built with 11ty - {{ "now" | date: "%Y-%m-%d %H:%M" }} -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="King's Digital Lab, King's College London">
    <meta name="description" content="{{ description }}">
    {% comment %}
      TODO: override the og: for individual pages (e.g. blog posts will have its own image, desc)
      TODO: Add twitter meta?
    {% endcomment %}
    <meta property="og:image" content="{{ metadata.live_domain }}{{ pageImageWebPath }}">
    <meta property="og:description" content="{{ description }}">
    <meta property="og:title" content="{{ title }}">

    {% comment %}
      <!-- Twitter -->
      <meta name="twitter:card" content="summary">
      <meta name="twitter:site" content="@">
    {% endcomment %}

    <link rel="icon" href="{{ '/assets/img/logos/lov-temp-logo.png' | url }}" type="image/x-icon">
    <title>{{ title }}
      |
      {{ metadata.title }}
    </title>
    {% comment %}
      generated from assets/css/main.scss
    {% endcomment %}
    <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}?ts={{ "now" | date: "%s" }}">
    <script src="{{ '/assets/js/navbar-toggle.js' | url }}?ts={{ "now" | date: "%s" }}"></script>
  </head>
  <body class="{{ body_class }}">
    {% include "header.html" %}
    <section class="section">
      <div class="container">
        <h1 class="title">
          {{ title }}
        </h1>
        <div class="content {{ tags }}">
          {{ content }}
        </div>
      </div>
    </section>
    {% include "footer.html" %}
  </body>
</html>
